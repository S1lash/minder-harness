# Profile — personalize this ONE file

> **This is the only file you personalize.** The generic canon in `rules/` and `doctrine/` stays
> untouched — it is the standard, shared by every fork. This file is where YOUR identity,
> domain, and presentation preferences live, so the agent adapts to you without any generic rule
> being edited. Fill in the placeholders; delete guidance lines you don't need.
>
> The agent reads this every session (it's hot). When a generic rule and your profile conflict on
> a matter of *presentation or preference*, your profile wins — it is the calibration layer on top
> of the floor. It never overrides a load-bearing HARD RULE (SoT, grounding, no-corner-cutting,
> safety); those are the floor for everyone.

## Who you are

- **Name:** <your name>
- **Role / what you do:** <e.g. solo founder / engineer / researcher / writer>
- **Domain(s):** <the fields you work in — what the agent should assume you know and care about>
- **Location / timezone:** <resolve time-sensitive things from here, don't hardcode elsewhere>

## How you want information presented (your deltas on top of rules/communication.md)

The baseline (conclusion-first, plain-language, no fluff, no sycophancy) always holds. Add YOUR
specifics — how praise, criticism, and density should land for you. Examples of the *kind* of
thing to write (replace with your own):

- **Praise:** <e.g. facts → conclusion, no evaluative labels; I reject empty praise>
- **Criticism:** <e.g. only concrete + provable + actionable — observation → pattern → consequence
  → what to do>
- **Density:** <e.g. ~80% interpretation / 20% event — I consume information to understand, not to
  relive the process>
- **Long-form:** <if you consume standalone reports / audiobooks / briefings, note your recipe —
  density, chapter cadence, hard bans>
- **Language:** <the language you think in; the agent answers in the language you ask in, code
  stays English>

## Working preferences

- **How much to plan vs just do:** <e.g. for anything non-trivial, show me a short plan first>
- **Autonomy:** <e.g. save knowledge silently and tell me in one line; don't ask permission for
  routine captures>
- **What you want the harness to absorb from your life:** <the recurring frictions you'd like it
  to automate / track — this feeds the concierge pillar, rules/harness-stewardship.md>
- **Anything the agent should never do without asking:** <your hard stops>

## Domain pointers

For each ecosystem / project area you work in, add a thin pointer file under `pointers/` and list
it here — so the agent knows the area exists and where to go, without loading its detail every
session:

- `pointers/<ecosystem>.md` — <one line: what it is, where its canon lives>

Keep this file tight. It is your calibration, not a wiki — deep domain knowledge lives in
`knowledge/` and in the pointer targets, not here.
