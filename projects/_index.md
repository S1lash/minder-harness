# Projects — index

> The things the person builds. They live in `projects/` inside this base, so every surface that
> has the base has them. The agent maintains this map. Where a project lives, what its own contract
> holds, and when it may be given its own repository: `../doctrine/project-home.md`.

## Active

_(none yet)_

<!-- one row per project:
| Project | What it is | Lives in |
|---|---|---|
| `<name>/` | one line | this base |
| `<name>/` | one line | its own repository — <url> |
-->
