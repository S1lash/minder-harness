# Attribution & Acknowledgements

`frontend-crafter` is a synthesis. The pipeline shape, provenance branching, durability-split
enforcement, persistence layer, and the anti-slop discipline are the author's. The shoulders it
stands on:

1. **Anthropic's official `frontend-design` skill** (Prithvi Rajasekaran, Alexander Bricken) —
   base philosophy: hero-as-thesis, ground-in-subject, writing-in-design, the named-AI-defaults
   calibration. This plugin **supersedes** it by absorbing that philosophy (see SKILL.md coexistence).

2. **Emil Kowalski — animations.dev skills** (`emil-design-eng`, `animation-vocabulary`,
   `review-animations`) — the motion craft engine (`reference/motion.md`), the ~120-term motion
   vocabulary, and the motion-review posture (`skills/motion-review/`). The single largest capability
   import.

3. **pbakaus/impeccable** (Apache-2.0) — the brand/product register split, quantified slop
   signatures (OKLCH cream-tell, reflex-reject font list), the 100/300/500 + 80ms motion framework,
   the OKLCH color recipe (60/30/10, tinted neutrals), the 8-interaction-states + undo>confirm +
   skeleton>spinner rules, and the "design intensity as an axis" verbs (`bolder`/`quieter`/`distill`).

4. **leonxlnx/taste-skill** — the numeric dials (DESIGN_VARIANCE / MOTION_INTENSITY / VISUAL_DENSITY),
   the committed-aesthetic-direction menu (base + delta), and the laziness-research framing that
   justifies "emit an inspectable commitment before output."

5. **Google Stitch** (stitch.withgoogle.com) — the iOS `100dvh` fix, `clamp()` responsive formulas,
   44px touch targets, transform/opacity-only motion, the state catalog (loading/empty/error),
   concrete font alternatives. Preserved in `reference/contracts/*` and `interaction-states.md`.

6. **OpenAI — "Designing Delightful Frontends with GPT-5.4"** (Mar 2026) — content discipline,
   AI-slop reduction patterns.

7. **GoogleChrome/modern-web-guidance** (Google Chrome + Microsoft Edge teams) — the Performance,
   Dark Mode, Forms, Accessibility, CSS Architecture, and Security contracts.

8. **Trystan-SA/claude-design-system-prompt** (MIT) — the five procedural skills in `procedures/`
   (discovery-questions, aesthetic-direction, generate-variations, make-tweakable, polish-pass). The
   `procedures/` folder mirrors these under their MIT license, preserved verbatim.

Ideas are not copyrightable; specific expression is. Where this work paraphrases, it credits. Where
it copies verbatim (the `procedures/` folder), it preserves upstream attribution and license.
